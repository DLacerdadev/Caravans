// Ponto de entrada do app web — preenchido na fase Foundational (T020).
import { createRoot } from 'react-dom/client';

createRoot(document.getElementById('root')!).render(
  <div style={{ padding: 24, fontFamily: 'system-ui' }}>caravans web — placeholder</div>,
);
